# Configuration for dictionary_recoder scripts.
# Edit the values to match your environment before running the scripts.

# Base directory of this repository.
REPO_DIR="${HOME}/Documents/dictionary_recoder"

# Data/log paths used elsewhere.
WORD_LOOKUP_LOG="${REPO_DIR}/word_lookup_log.tsv"

# Autopush script and log paths.
AUTOPUSH_SCRIPT="${REPO_DIR}/autopush.sh"
AUTOPUSH_AGENT_LOG="${REPO_DIR}/autopush.agent.log"
AUTOPUSH_STDOUT_LOG="${REPO_DIR}/autopush.out.log"
AUTOPUSH_STDERR_LOG="${REPO_DIR}/autopush.err.log"
AUTOPUSH_LOCK_DIR="/tmp/dictionary_recoder_autopush.lock"

# LaunchAgent configuration (24h clock).
LAUNCH_AGENT_LABEL="com.dictionaryrecorder.autopush"
LAUNCH_AGENT_PATH="${HOME}/Library/LaunchAgents/${LAUNCH_AGENT_LABEL}.plist"
LAUNCH_START_HOUR=12
LAUNCH_START_MINUTE=0

# PATH used by autopush.sh when running git.
AUTOPUSH_PATH="/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin"
